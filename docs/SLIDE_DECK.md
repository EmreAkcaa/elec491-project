# StoNeCoAl Final Presentation — Slide Deck Outline

**Format:** 13 slides, ~15 minutes total (~1 min per slide + 1 min for Q&A
warm-up). Times are upper bounds; the demo (slide 12) is the longest stop.

**Audience:** A different professor than the supervisor — finance-leaning,
bullshit-allergic. Lead with the most novel finance result (the base currency
sensitivity finding) at slide 5, anchor the EEE narrative on the system
schematic at slide 3, demote the philosophical bits. Every claim is
either a published number from the literature or a number generated by
the live pipeline; nothing is invented.

**Source numbers:** Every figure in the speaker notes is keyed to a live
artifact under `data/<market>/results/` or to `docs/EEG_VERIFICATION.md`.
Regenerate anything by running `uv run python run_pipeline.py` (or
`scripts/run_basis_variants.py`) and re-reading the JSON / parquet.

**The story arc:** *Same 12-stage pipeline, three sensor streams.* The
demo opens on the cross-market BIST↔S&P comparison, hits the base currency
finding (refutes the "FX-stripping cleans the market" prior), shows the
information-theory layer as the multivariate summary that ties it all
together, and closes with EEG to prove the toolkit is sensor-agnostic.

---

## 1. Title + headline sentence (45 s)

**Title:** *StoNeCoAl — Stock Network Correlation Analysis*
*A reproducible multi-domain signal-processing pipeline applied to
financial and biomedical sensor data.*

**Bottom of slide:** Authors · ELEC 491 · Koç University · 2026

**Speaker note:** "We applied the same 12-stage signal-processing chain
to three sensor streams: BIST 100, S&P 500, and 64-channel motor-imagery
EEG from PhysioNet. The pipeline produces sector-coherent networks on
both markets and anatomically plausible structure on EEG."

---

## 2. Midterm response — what changed since 65/100 (60 s)

**Three bullets, large font:**

1. **Supervisor:** *"not an EEE project, no hardware."* → Our response:
   the sensor + DSP chain *is* the EEE engineering. New system schematic
   (slide 3) makes that explicit.
2. **TA:** *"add an information theory perspective."* → Built a full IT
   layer (MI matrix, D_eff, ΔH, KL divergence). Slide 8.
3. **Three real bugs were silently corrupting the proposal's primary
   validation criterion.** All fixed. Slide 11.

**Speaker note:** "We're not here to defend the midterm score; we're
here to show the response. Concrete and measurable."

---

## 3. System schematic — the chain (90 s)

**Figure:** `docs/figures/system_schematic.svg` full-page.

**Speaker note (pointing at boxes top-to-bottom):**
> "Sensors → acquisition → statistical signal processing → information
> theory → network extraction → optional neuromorphic inference →
> visualisation. Every stage is real EEE: 160 Hz ADC, bandpass + notch
> + common-average reference DSP, random matrix theory, Shannon
> entropy, transfer entropy, spike-coded inference. No hardware
> fakery — the EEG cap is a literal biomedical sensor (PhysioNet
> BCI2000); the exchange tick aggregates are sensor-derived
> economic data."

---

## 4. BIST result — the proposal sanity check (75 s)

**Figure:** MST coloured by sector + the ARI / NMI KPI strip from the
hero panel on Market Overview.

**Numbers:**
- 73 surviving tickers after 90% coverage filter
- 20 Ward clusters
- **ARI = 0.271, NMI = 0.719** vs official Borsa İstanbul sectors
- 6 of 7 major banks (AKBNK, GARAN, YKBNK, VAKBN, HALKB, ISCTR) share
  cluster 1; SKBNK splits off (Şekerbank — much smaller free float)

**Speaker note:** "The proposal's primary validation criterion is that
the MST recovers known sectors. Ward linkage on Mantegna distance
recovers Borsa Istanbul's sector classification with ARI = 0.27 and NMI
= 0.72, and the banking sanity check passes for 6 of 7 major banks.
Conglomerates separate cleanly, energy / steel / beverages each form
their own clusters. The MST topology is conglomerate-led — KCHOL, SISE,
SAHOL are the dominant hubs, which matches the actual structure of the
Turkish equity market."

---

## 5. Base-currency experiment — the headline finding (105 s) ⭐

**Figure:** the 3-bar eigenvalue-spectrum overlay + KPI strip from the
Base currency Sensitivity panel on the Cross-Market page. Optional second
panel: the sector-purity per-base-currency bars (slide 7 detail).

**Numbers:**

| Base currency | D_eff | Avg ρ | Top eig share |
|---|---:|---:|---:|
| TRY | 6.30 | 0.374 | **38.87%** |
| USD | 4.77 | 0.436 | **45.11%** |
| Gold | 3.71 | 0.502 | **51.47%** |

**Speaker note:** "The naïve prior is that re-expressing BIST returns
in a stable foreign base currency — USD or gold — should *strip out* a
common factor and reduce the top eigenvalue's variance share. We took
the full BIST adjusted-close panel, divided every ticker's price by
USDTRY (and separately by gold-in-TRY), recomputed log-returns, and ran
the full 12-stage pipeline on each variant. The data says the
*opposite*: top eigenvalue share rises from 38.9% (TRY) to 45.1% (USD)
to 51.5% (gold), and the effective dimensionality drops from 6.3 to
3.7. The interpretation is finance-substantive: TRY volatility is a
**dispersion source** for BIST equities — exporters benefit when the
Lira weakens, importers and rate-sensitive financials suffer, so the
currency leg creates *cross-sectional spread* rather than common
movement. Strip the currency and you uncover a single dominant global-
equity-risk factor that TRY noise was masking. This is the kind of
empirical question multi-base-currency portfolios actually face. We tested
it; the prior failed."

---

## 6. Cross-market BIST vs S&P (90 s)

**Two MSTs side-by-side, both sector-coloured.** Plus a 2-column KPI
strip:

| Metric | BIST | S&P 500 |
|---|---:|---:|
| Surviving tickers | 73 | 485 |
| ARI vs sector | 0.27 | **0.31** |
| NMI vs sector | **0.72** | 0.60 |
| D_eff | **6.30** | **6.56** |
| Avg pairwise ρ | 0.374 | 0.220 |
| Top eigenvalue share | 38.9% | ~18% |
| Türkiye-quake spike (mean abs corr) | **0.44 → 0.66 → 0.58** | 0.44 → 0.35 → 0.31 (flat) |

**Speaker note:** "Same 12-stage pipeline, two markets at opposite ends
of the emerging-vs-developed dimension. Notice the **invariant** first:
effective dimensionality D_eff lands within ~5% across two unrelated
markets (6.30 BIST vs 6.56 S&P) despite 6.6× difference in N. That's a
non-trivial cross-market regularity. Notice the **divergence** second:
the Feb-2023 Türkiye earthquake spikes BIST's mean correlation from
0.44 to 0.66 during the event window; the S&P stays flat. Our toolkit
catches market-specific stress events as local features, not generic
noise — exactly what cross-market dependence analysis should do."

---

## 7. Base-currency decomposition — *what factor structure shifts* (75 s) ⭐

**Figure:** `docs/figures/numeraire_sector_shift.svg` (two-panel:
variance share per mode + banking-sector mass per mode, all three
base currencies colour-coded).

**Numbers** (regenerated live from
`scripts/numeraire_sector_decomposition.py`):

| Mode | TRY | USD | Gold | Identity (consistent across all three) |
|---|---:|---:|---:|---|
| #1 (market) | **38.87%** | **45.11%** | **51.47%** | Conglomerate-led; top tickers KCHOL · SAHOL · SISE · THYAO |
| #2 (banking) | 4.16% | 3.60% | 3.23% | **62% / 60% / 61% of mode-mass is BIST banks** (GARAN · AKBNK · ISCTR · YKBNK · VAKBN) |
| #3 (old industrials) | 2.51% | 2.17% | 1.92% | BRYAT · BRSAN · TUPRS · FROTO |

**Two findings, one slide:**

1. **Mode #1 (the market factor) absorbs *more* variance under USD/Gold**
   — the "everything-moves-together" mode strengthens 12.6 percentage
   points when the currency leg is stripped.

2. **Mode #2 is a genuine banking factor under *every* base currency** —
   5 of 73 BIST tickers (6.8% population baseline) carry ~60% of mode 2's
   variance: a **9× over-representation** that exists in TRY, USD and Gold
   alike. The mode itself *weakens* 22% in variance share (4.16% → 3.23%)
   under USD/Gold, supporting the read that the bank-vs-market spread is
   largely TRY-rate-driven: take the rate channel out and banks merge
   slightly into the broad market mode.

**Speaker note:** "The headline base currency result (slide 5) shows the top
eigenvalue's share rises when we strip the currency leg. Decomposing
the spectrum into its top-3 modes shows *how the variance moves around*.
Mode 1 — the everything-together mode dominated by Koç Holding and
Sabancı Holding — gains 12 percentage points. Mode 2 is a pure banking
factor that survives all three base currencies: 5 of 73 tickers carry 60% of
its variance, 9× their population weight. Under USD or Gold, this
banking-orthogonal factor *weakens* in variance share, which is
consistent with banks' rate sensitivity being predominantly a TRY-side
phenomenon — once you strip the currency, the bank-vs-market spread
shrinks toward the market mode."

---

## 8. Information Theory layer — cross-universe view (60 s)

**Figure:** 4-panel composite from the new IT sub-tab —
KPI strip (D_eff / ΔH / sign-entropy) · MI vs Pearson scatter ·
rolling D_eff(t) · regime KL table.

**Lead with cross-universe D_eff invariance:**

| Universe | N | D_eff | ΔH (nats) | Mean sign-entropy |
|---|---:|---:|---:|---:|
| BIST 100 | 73 | **6.30** | 21.97 | 0.996 bits/day |
| S&P 500 | 485 | **6.56** | 307.30 | 0.997 bits/day |
| EEG (motor) | 64 | **4.69** | (singular) | 0.743 bits/day |

Two financial markets land within 5% of each other on D_eff despite a
**6.6× difference in N** — a non-trivial cross-market regularity. EEG
sits substantially below at 4.69, consistent with brain dynamics being
more concentrated (a few dominant rhythms vs the equity-market
diversification across sectors).

**Mean sign-entropy ≈ 1 bit/day on both equity markets** — the
canonical weak-form-EMH fingerprint: tomorrow's direction is
informationally independent of today's, simultaneously for *every*
ticker, *every* universe. EEG comes in lower at 0.74 bits/day because
brain activity *is* more deterministic than equity returns — there are
rhythms.

**Regime KL** quantifies joint-distribution shift during crises:

| Crisis | BIST KL (nats) | S&P KL (nats) | Ratio Türkiye/Ukraine |
|---|---:|---:|---:|
| Ukraine invasion | 287 | 22 862 | — |
| Türkiye earthquake | 531 | 41 663 | **1.85× (BIST), 1.82× (S&P)** |

The Türkiye/Ukraine ratio is **the same on both markets**, even though
the earthquake physically only hit Türkiye. The KL is picking up the
2023 global macro regime shift, not just the local event.

Plus: pairwise MI catches **14 non-linear-excess pairs** on BIST that
the linear Pearson method misses (top pair BRSAN-HEKTS at 0.044 bits
above the Gaussian baseline).

**Speaker note:** "Same 12 stages produce a multivariate summary of
the joint distribution in nats and bits, across all three universes.
The headline cross-universe finding: effective dimensionality is ≈ 6 on
both equity markets and ≈ 4.7 on EEG — three independent datasets, two
domains, a single quantitative anchor. Sign-entropy ≈ 1 bit/day across
all tickers is the canonical EMH fingerprint. Regime KL during the
Türkiye earthquake is 1.85× the Ukraine invasion on BIST — but the
*same* 1.82× ratio shows up on S&P even though the quake didn't
physically affect the US, telling us the KL is picking up the 2023
global macro regime shift, not just the local event."

---

## 9. EEG — methodology portability (60 s)

**Figure:** Anatomical MST for EEG + the sanity-check report table from
`docs/EEG_VERIFICATION.md`.

| # | Check | Status |
|---|---|---|
| 1 | Inter-hemispheric homologous coherence | **PASS** (Fp1~Fp2 r=0.86, O1~O2 r=0.85) |
| 4 | Posterior MST hubs | **PASS** (Pz, Oz in top-5) |
| 5 | TE(F→C) > TE(C→F) | **PASS** |
| 3 | RMT signal mode | PARTIAL (λ₁=27 ≫ MP=1; ratio 2.93 vs 5 target) |
| 2 | Motor-imagery desynchronisation | future work (3.4 GB raw cache) |

**Speaker note:** "Same 12-stage pipeline applied to PhysioNet 64-
channel motor-imagery EEG, *no code changes*. Homologous hemispheric
pairs co-cluster, midline hubs dominate the functional MST, and
transfer-entropy directionality matches the canonical frontal-to-motor
information flow. Three of four executable sanity checks pass cleanly;
the RMT λ₁/λ₂ ratio is below the equity-market target because EEG has
multiple comparably-strong rhythms (alpha, beta, mu) instead of a
single dominant common factor — a known neuroscience result, not a
methodological failure. The pipeline is sensor-agnostic."

---

## 10. Engineering depth — what we built (60 s)

**Three columns:**

| Custom modules | Pipeline stages | Datasets driven |
|---|---|---|
| `src/info_theory.py` (MI, D_eff, ΔH, KL, rolling D_eff, sign-entropy) | 13 stages, single-command runs | BIST 100 (73 tickers, 5 yr daily) |
| `src/numeraire_acquisition.py` + `src/basis_transform.py` (FX/gold variant generator) | All stages reproducible from raw | S&P 500 (485 tickers, 5 yr daily) |
| `src/transfer_entropy.py` — circular-block-bootstrap surrogate + Benjamini–Hochberg FDR correction (in-house, no statsmodels dep) | 196 unit tests, 100% green | BIST/USD + BIST/Gold variants |
| 5 dashboard pages × N sub-tabs in Streamlit | Per-market `data/<m>/{raw,processed,results}/` tree | PhysioNet EEG (64 channels, 10 subjects pooled) |

**Speaker note:** "Three custom modules implementing
graduate-level signal-processing methods, 13 pipeline stages with
single-command reproducibility across 5 universes, 196 unit tests, all
on a git branch with one commit per logical change. The block-
bootstrap surrogate and BH-FDR routines are written from scratch to
avoid adding heavy dependencies — the math comes straight from Politis
& Romano 1992 and Benjamini & Hochberg 1995."

---

## 11. Engineering rigour — what we fixed (60 s)

**Three bullets:**

1. **Clustering pathology** — switched single-linkage chaining to
   Ward+20. BIST max cluster 45→12; S&P max cluster 446→36; ARI 8×
   higher (BIST) and 44× higher (S&P). The proposal-promised "banks
   cluster together" sanity check now passes.
2. **TE methodological hardening** — replaced `np.random.permutation`
   surrogate (destroyed source autocorrelation) with a circular-block-
   bootstrap (Politis & Romano 1992, block length 5). Added
   Benjamini–Hochberg FDR correction across the N×(N−1) directed pairs.
   Both fixes are config-driven via `transfer_entropy.surrogate_block_length`
   and `transfer_entropy.multiple_testing`.
3. **Base-currency experiment infrastructure** — `src/basis_transform.py`
   exposes a single `apply_numeraire` function with a mathematical
   identity test (the chain rule holds: `log(P/B).diff() ≡
   log(P).diff() − log(B).diff()`). Variants register as first-class
   universes in `app/universe_registry.py` with their own dashboard
   path.

**Reproducibility:**
- **196 tests passing**, single-command runs
  (`uv run python run_pipeline.py`)
- **5 universes** registered and rendered (BIST, S&P, BIST/USD,
  BIST/Gold, EEG)
- **Reproducible** end-to-end from raw data

---

## 12. Live demo (90 s — longest stop) {#demo}

**Demo script — rehearse exactly these clicks, exactly these
sentences:**

| Step | Action | Sentence |
|---|---|---|
| 1 | Open dashboard, land on Cross-Market | "Central question: how does BIST co-movement compare to a developed market like the S&P 500?" |
| 2 | Point to the Türkiye-quake row in the headline KPI strip | "BIST is conglomerate-led and stress-reactive; the Feb-2023 earthquake spike is isolated to BIST and invisible on the S&P." |
| 3 | Scroll to the Base currency Sensitivity sub-section | "When we re-express BIST in USD or gold, the top eigenvalue share *rises* from 39% to 51%, and the effective dimensionality drops from 6.3 to 3.7. TRY noise was masking a global-equity common factor." |
| 4 | Switch universe to EEG via sidebar | "Same 12-stage pipeline on a 64-channel motor-imagery EEG cap. No code changes — anatomical clusters emerge automatically." |
| 5 | Click EEE Analysis → Information Theory sub-tab | "The IT layer gives the multivariate distribution summary: MI in bits, D_eff, joint entropy, regime KL." |

**Backup:** Pre-recorded demo video on a USB stick. If anything crashes
on the room's machine, switch to video and keep narrating.

---

## 13. Contributions + future work (45 s)

**Three contributions:**

1. **Empirical:** the base-currency experiment — TRY-stripping *increases*
   BIST's common-mode share (38.9% → 51.5%), refuting the naïve prior
   and identifying TRY as a cross-sectional dispersion source rather
   than a common factor.
2. **Methodological:** an information-theoretic layer that summarises
   the joint distribution in nats and bits — pairwise MI, effective
   dimensionality, ΔH, and regime KL — complementing the existing
   network methods with a quantitative multivariate view.
3. **System:** the same 12-stage pipeline running on three sensor
   streams (BIST, S&P 500, 64-channel EEG) with one-command
   reproducibility and 196 unit tests.

**Future work:** OOS pair-trading backtests with transaction costs,
motor-imagery decoding on the EEG cap, real-time streaming dashboard,
physical Loihi-2 deployment of the spike-coded inference layer.

---

## Q&A backup slides (only if asked)

- A1: Why Ward + n_clusters=20? (Tested against the proposal's "banks
  cluster together" sanity check; Ward with maxclust criterion gives
  balanced cluster sizes and recovers sector structure cleanly.
  Fallback `complete` linkage tested as backup.)
- A2: Why BH-FDR not Bonferroni? (BH controls the *expected proportion*
  of false discoveries among rejections at level α rather than the
  family-wise error rate; appropriate when many of the hypotheses are
  expected to be true.)
- A3: Why XAU/TRY for the gold base currency, not XAU/USD? (We compare
  BIST equity returns expressed in three units measuring local
  purchasing power: TRY, USD, and a hard asset. XAU/USD would conflate
  the FX swap with the gold leg.)
- A4: Why is D_eff invariant across BIST and S&P? (Both are
  diversified large-cap indices with ~3-4 dominant cross-sector modes
  plus a few isolated industries. The invariance suggests `D_eff ≈ 6`
  is a structural property of "modern diversified equity index" rather
  than a property of either specific market.)
- A5: What runs on Loihi 2 in practice? (Currently nothing — Loihi/
  TrueNorth/SpiNNaker are the target deployment substrates. The
  architecture is already neuromorphic-compatible: Σ-Δ-style spike
  encoders, LIF dynamics, recurrent topology with surrogate-gradient
  training. The readout layer would need rate-to-spike conversion
  before physical deployment.)
- A6: Why the regime KL ratio Türkiye-quake / Ukraine = 1.85? (Both
  markets show the same ~1.83× ratio, so the KL is picking up the 2023
  global macro regime shift rather than the local earthquake — Türkiye
  Q1-2023 coincided with a banking-stress wave, oil-price re-rating,
  and Fed-pivot pricing that hit equity covariances globally. Honest
  read: KL is a sensitive *aggregate* measure of joint-distribution
  shift, and 2023's first quarter was a real regime change for global
  equities regardless of which event triggered it.)
- A7: Isn't BIST mostly financials? (No — the top eigenvector mass under
  TRY breaks down as Conglomerates 11.0%, Banking 10.2%, Energy 8.9%,
  Automotive 8.0%, Retail 6.4%. The market factor is a balanced
  diversified large-cap mode, not a banking-dominated one. The
  banking-orthogonal factor lives in mode #2 at ~60% bank mass — that's
  the mode where banks behave as one block separate from the broad
  market.)
- A8: How does cluster purity change across base currencies? (TRY: 33% mean
  cluster purity; USD: 40%; Gold: 41%. Removing the currency leg gives
  *cleaner* sector recovery — another angle on the same story as the
  rising top-eigenvalue share. Both metrics point at "the broad-market
  / sector factor structure is clearer once FX noise is gone".)

---

## Production checklist

- [ ] Build the schematic SVG (`uv run python scripts/draw_system_schematic.py`)
- [ ] Generate the per-sector base-currency decomposition figure
      (`uv run python scripts/numeraire_sector_decomposition.py`)
- [ ] Pull the live dashboard screenshots for slides 4, 5, 6, 7, 8, 9
- [ ] Boot the dashboard on the room's machine ≥ 24 h before final
- [ ] Record a 90-second demo video as backup
- [ ] Time the full deck end-to-end at least 5 times
- [ ] Print 5 handout copies of the abstract + 4-row results table
