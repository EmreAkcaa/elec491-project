# Runtime dependencies for the Streamlit dashboard (mirrors pyproject.toml
# dependencies = [...] for deploy targets that read requirements.txt:
# Hugging Face Spaces, Streamlit Cloud, Render, Fly.io, etc.).
#
# Pinned versions where Streamlit Cloud / HF Spaces compatibility matters;
# loose >= for everything else.

# Core data stack
pandas>=2.2
numpy>=1.26
scipy>=1.13
pyarrow>=16.0
pyyaml>=6.0

# Streamlit + Plotly stack
#   streamlit >= 1.41 because dashboard.py uses st.segmented_control which
#   was added in Streamlit 1.41 (Nov 2024). HF Spaces' sdk_version in
#   README.md frontmatter must also be >= 1.41.0 — keep these two in sync.
#   plotly pinned <6 because plotly 6 + kaleido 1.x require a system Chrome
#   install for static-image export. Streamlit Cloud / HF Spaces don't have
#   Chrome → static export crashes. 5.x has every feature this project uses.
streamlit>=1.41,<2
plotly>=5.22,<6
#   kaleido 0.2.1 bundles its own Chromium; kaleido 1.0+ delegates to system
#   Chrome via plotly_get_chrome which we can't trigger on hosted envs. Pin.
kaleido==0.2.1

# Network + clustering
networkx>=3.2
scikit-learn>=1.5

# Domain analysis
PyWavelets>=1.6

# Acquisition (pipeline-side; not strictly needed for dashboard at runtime,
# but kept here so a "pipeline-run-on-deploy" path stays available without
# a second requirements file).
yfinance>=0.2.40
isyatirimhisse>=2.1

# Hugging Face Hub client — used by app/dashboard.py to fetch the EEG bulk
# parquets from the companion Dataset repo on first Space launch (the Space
# repo itself is capped at 1 GB so the parquets live in a separate Dataset).
# Also used by scripts/upload_eeg_to_hf_dataset.py for the one-time upload.
huggingface_hub>=0.24
